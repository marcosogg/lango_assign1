<table class="table is-fullwidth">
  <thead>
    <tr>
      <th>Place</th>
      <th>Category</th>
      <th>Description</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each collection.places}}
    <tr>
      <td>{{place}}</td>
      <td>{{category}}</td>
      <td>{{description}}</td>
      <td>{{lat}}</td> <!-- Display latitude -->
      <td>{{long}}</td> <!-- Display longitude -->
      <td>
        <a href="/collection/{{../collection._id}}/deleteplace/{{_id}}" class="button is-danger">
          <i class="fas fa-trash"></i>
        </a>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>
